class AppRoutes {
  // Customer name screen — first screen when the app launches
  static const customerNameScreen = '/customer-name';

  // QR code screen — user scans QR to open the menu
  static const qrCode = '/qrcode';

  // Menu page — list of available food & drinks
  static const menu = '/menu';

  // Menu detail page — shows details of selected item
  static const menuDetail = '/menu-detail';

  // Cart page — shows all items before checkout
  static const cart = '/cart';

  // Orders page — shows list of placed orders
  static const orders = '/orders';
}
